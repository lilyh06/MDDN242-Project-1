# MDDN242-Project-1

I started by getting the variables to be at the top of the css file so it was easier to work with. Then added different hand drawn background images to test out the style that would work the best.

Using Ai I prompeted it to see how i could change the css file to make my cursor an image.
"Could you change the cursor to a image called image.png using this css file:..."
I used it to debug the code as it started working but only showing sections of the image. I found out a cursor image could only be 128x128px maximum to run on chrome.

I wanted the website to still show some of my personal style somehow since its a portfolio website, but still be pretty clear to navigate.

Prompting an A.I (co-pilot) to add 3 centered buttons using .png images of fish I had draw, as fish are something i often draw stylized in different ways.

The colours are bold, and the idea is simple but also bold so i need the title of the website to reflect that
